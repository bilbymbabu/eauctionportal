apiVersion: v1
kind: Service
metadata:
  name: {{ template "eauctiongetway.fullname" . }}
  labels:
    app: {{ template "eauctiongetway.name" . }}
    chart: {{ template "eauctiongetway.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "eauctiongetway.name" . }}
    release: {{ .Release.Name }}
